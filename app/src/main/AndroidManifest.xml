<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.firna.loakyuk2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Loginactivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".user_profile_image_background" />
        <activity
            android:name=".dashboarduser"
            android:label="@string/title_activity_dashboarduser"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".hargajual" />
        <activity android:name=".jual_user" />
        <activity android:name=".settings" />
        <activity android:name=".login" />
        <activity android:name=".register" />
        <activity android:name=".editprofile" />
        <activity android:name=".jualcepat"></activity>
    </application>

</manifest>